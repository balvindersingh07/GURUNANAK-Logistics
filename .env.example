# GURUNANAK Logistics — Environment Variables (names only)
# Copy to .env in project root and server/.env as needed

# Backend
JWT_SECRET=
MONGODB_URI=
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
EMAIL_PROVIDER=

# Optional media upload (omit for demo/local proof storage)
CLOUDINARY_URL=

# Frontend (Vite)
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000

# Testing
SKIP_DB_TESTS=0

# Load testing (k6)
BASE_URL=http://localhost:5000
AUTH_TOKEN=
